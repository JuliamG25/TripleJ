---
import { Button } from '../islands/Button';
---

<section class="py-20 bg-primary">
  <div class="container mx-auto px-4">
    <div class="mx-auto max-w-3xl text-center">
      <h2 class="text-3xl font-bold tracking-tight text-primary-foreground mb-4 md:text-4xl">
        Comienza a gestionar tus proyectos hoy
      </h2>
      <p class="text-primary-foreground/80 text-lg mb-8">
        Únete a la comunidad FESC y lleva tus proyectos académicos al siguiente nivel 
        con nuestras herramientas de gestión colaborativa.
      </p>
      <div class="flex flex-col items-center justify-center gap-4 sm:flex-row">
        <Button 
          size="lg" 
          variant="secondary" 
          client:load
          href="/login"
          className="gap-2 px-8 bg-white text-primary hover:bg-white/90"
        >
          Acceder al Sistema
          <svg class="h-4 w-4 inline" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3" />
          </svg>
        </Button>
        <Button 
          size="lg" 
          variant="outline" 
          client:load
          className="border-primary-foreground/30 text-primary-foreground hover:bg-primary-foreground/10 hover:text-primary-foreground bg-transparent"
        >
          Contactar Soporte
        </Button>
      </div>
    </div>
  </div>
</section>
